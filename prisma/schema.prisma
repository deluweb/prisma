generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Account {
  email    String
  provider String
  token    String

  websiteId String
  userEmail String
  User      User   @relation(fields: [websiteId, email], references: [websiteId, email])

  @@id([provider, email])
}

model Session {
  id        String   @id
  expires   DateTime
  createdAt DateTime @default(now())
  token     String
  tokenType String?
  updatedAt DateTime

  websiteId String
  userEmail String
  User      User     @relation(fields: [websiteId,userEmail], references: [websiteId, email])
}

model User {
  name      String
  email     String
  image     String?
  role      String @default("USER")
  websiteId String

  Website   Website   @relation(fields: [websiteId], references: [id])

  Account   Account[]
  Session   Session[]

  @@id([websiteId, email])
}

model Website {
  id        String   @id
  name      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime

  domains   String[]
  User      User[]
}
